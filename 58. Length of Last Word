class Solution {
    public int lengthOfLastWord(String s) {
      
        String []str=s.split(" ");
        int len=str.length;
        if(len>=1)return str[len-1].length();
        return 0;
    }
}
